Form field for inquiry / application forms.

```jsx
<Input label="Full Name" icon="fa-solid fa-user" placeholder="Jane Doe" />
<Input label="Message" textarea placeholder="How can we help?" />
```

Set `textarea` for multi-line. Pairs with `Button variant="primary"` for submits.
