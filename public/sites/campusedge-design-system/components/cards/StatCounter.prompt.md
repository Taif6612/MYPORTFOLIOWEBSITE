Animated statistic that counts up the first time it scrolls into view.

```jsx
<StatCounter value={15000} suffix="+" label="Students Enrolled" />
<StatCounter value={95} suffix="%" label="Graduate Employment" onDark />
```

The suffix renders in gold. Use `onDark` on the navy stats band. Group 3–4 in a flex/grid row.
